server:
  port: 40070
  servlet:
    context-path: /api/v1

spring:
  application:
    name: api-alumno-service-v1

  datasource:
#    url: jdbc:oracle:thin:@localhost:1521/XE
    url: jdbc:oracle:thin:@localhost:40071/XE
    username: SYSTEM
    password: plsapito
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.OracleDialect
    properties:
      hibernate:
        format_sql: true